apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "db-proxy.fullname" . }}
  labels:
    {{- include "db-proxy.labels" . | nindent 4 }}
data:
  config.yaml: |
    {{- .Values.config | toYaml | nindent 4 }}
